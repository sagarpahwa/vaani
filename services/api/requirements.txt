# Vaani coaching API (POC) — backend deps. Installed into .venv-poc only.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.7
pydantic-settings>=2.3
pymongo>=4.7
python-multipart>=0.0.9
httpx>=0.27

# test + lint (POC keeps these in one file for a single `make poc-api-install`)
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
mongomock>=4.1
ruff>=0.6
black>=24.0
